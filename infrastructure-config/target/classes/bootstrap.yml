spring:
  application:
    name: aiyingshiconfigserver01
  cloud:
    config:
      server:
        git:
          uri: http://192.168.100.63:9090/digitalo/infrastructure-cloud.git
          username: aiyingshi
          password:
    bus:
      refresh:
        enabled: true
  kafka:
    bootstrap-servers:
    customer:
      group-id:
management:
  endpoints:
    web:
      exposure:
        include: '*'
    
    
  